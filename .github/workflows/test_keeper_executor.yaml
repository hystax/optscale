name: Build keeper_executor test image and run tests on it
run-name: Build keeper_executor test image and run tests on it - started by ${{ github.actor }}
on:
  pull_request:
    types: [opened, synchronize]
    paths:
    - 'docker_images/keeper_executor/**'
  workflow_dispatch:


jobs:
  build_image:
    runs-on: self-hosted
    steps:
      - name: Check out code
        uses: actions/checkout@v3
      - name: Build image
        run: bash -x build.sh keeper_executor build
      - name: Build test image and run tests
        run: bash -x docker_images/run_test.sh keeper_executor
