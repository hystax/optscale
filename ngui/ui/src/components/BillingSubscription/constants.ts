export const YEARLY_PRICE_DISCOUNT = 0.2;

export const STRIPE_PLAN_NAME = Object.freeze({
  FREE: "Free",
  PRO_MONTHLY: "Pro monthly",
  PRO_YEARLY: "Pro yearly",
});

export const BILLING_CYCLE = Object.freeze({
  MONTHLY: "monthly",
  ANNUAL: "annual",
});

export const BILLING_SUBSCRIPTION_STATUS = Object.freeze({
  ACTIVE: "active",
  LIMIT_EXCEEDED: "limit_exceeded",
  SUSPENDED: "suspended",
});
