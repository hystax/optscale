import { Typography } from "@mui/material";
import { FormattedMessage } from "react-intl";

const AccessKeyTitle = () => (
  <Typography fontWeight="bold" gutterBottom>
    <FormattedMessage id="accessKey" />
  </Typography>
);

export default AccessKeyTitle;
