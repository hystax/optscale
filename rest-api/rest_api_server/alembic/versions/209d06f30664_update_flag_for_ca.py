""""update_flag_for_ca"

Revision ID: 209d06f30664
Revises: d093179183b5
Create Date: 2019-07-17 15:00:30.992439

"""
from alembic import op
import sqlalchemy as sa
from sqlalchemy.orm import Session


# revision identifiers, used by Alembic.
revision = '209d06f30664'
down_revision = 'd093179183b5'
branch_labels = None
depends_on = None


def upgrade():
    # ### commands auto generated by Alembic - please adjust! ###
    op.add_column('cloud_agent', sa.Column(
        'update_enabled', sa.Boolean(), nullable=False))

    ca_table = sa.table('cloud_agent', sa.Column(
        'update_enabled', sa.Boolean(), nullable=False))
    bind = op.get_bind()
    session = Session(bind=bind)
    enable_update = sa.update(ca_table).values(update_enabled=True)
    try:
        session.execute(enable_update)
        session.commit()
    finally:
        session.close()
    # ### end Alembic commands ###


def downgrade():
    # ### commands auto generated by Alembic - please adjust! ###
    op.drop_column('cloud_agent', 'update_enabled')
    # ### end Alembic commands ###
