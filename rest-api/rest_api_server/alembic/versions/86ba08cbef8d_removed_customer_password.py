""""removed_customer_password"

Revision ID: 86ba08cbef8d
Revises: 3907fe291cf2
Create Date: 2017-05-22 15:10:43.458233

"""
from alembic import op
import sqlalchemy as sa
from sqlalchemy.orm import Session
from sqlalchemy.sql import table, column
from sqlalchemy import update, String

# revision identifiers, used by Alembic.
revision = '86ba08cbef8d'
down_revision = '3907fe291cf2'
branch_labels = None
depends_on = None


def upgrade():
    op.drop_column('customer', 'password')
    # ### end Alembic commands ###


def downgrade():
    # ### commands auto generated by Alembic - please adjust! ###
    op.add_column('customer', sa.Column('password', sa.String(length=20),
                                        nullable=True))
    bind = op.get_bind()
    session = Session(bind=bind)
    customer_table = table('customer',
                           column('password', String(20)))

    update_password = update(customer_table).values(password='pass')
    try:
        session.execute(update_password)
        session.commit()
    finally:
        session.close()
    # ### end Alembic commands ###
