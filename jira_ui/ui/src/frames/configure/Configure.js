import React from "react";
import ConfigureContainer from "containers/ConfigureContainer";

const ConfigureFrame = () => (
  <div style={{ padding: "65px 120px 0px", width: "50%", minWidth: "500px" }}>
    <h1 style={{ marginBottom: "32px" }}>OptScale for Jira</h1>
    <ConfigureContainer />
  </div>
);

export default ConfigureFrame;
